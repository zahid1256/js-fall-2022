/** for (let i = 0; i<= 5; i++) {
    console.log('loop' + i);
}

first occurrence: 

for (let i = 0; i <= favSports_1.length-1 ; i++) {
    if (favSports_1[i].toLowerCase().localeCompare('soccer')=== 0) {
        indexAns = i;
        break;
    }
}
    console.log(indexAns); 


    for (let i = 0; i <= favSports_2.length-1 ; i++) {
        if (favSports_2[i].toLowerCase().localeCompare('baseball') === 1) {
            indexAns[i]} {
            console.log('baseball is only present once in the array');
            } 


let hour = 19

if (hour >= 6 && hour < 12) 
    console.log('good morning');
else if (hour >= 12 && hour <= 18) 
   console.log('good afternoon');
else 
   console.log('good evening');
            

let total = -1
for (let num = 0; num <= 10; num++) {
        total = total + num;
}
   
//console.log(total);


/** let fruits = ['mango', 'banana', 'apple']

for (let i = 0; i<= fruits.length-1; i++)
   console.log(fruits[i]); 
*///


 let student1_2 = {
        name : 'Eli hen',
        state : 'NY',
        age : 22,
        gender : 'F',
        paymentMethod : 'Paid',
        anyExperience : false,
        pastJobs : [],
        knownSkills: ['Java', 'Manual testing', 'CS basics']
    }

for (let elem in student1_2) {
    console.log(student1_2['age']);
    break;
}